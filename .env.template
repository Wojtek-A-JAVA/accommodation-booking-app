
# Security / JWT
# Long random string (at least 32+ chars). Do NOT commit real secrets.
JWT_SECRET=change-it-to-a-long-random-secret
# Token lifetime in milliseconds (example: 3600000 = 1 hour)
JWT_EXPIRATION_MS=3600000
# ----------------------------
# Payments (Stripe)
# Stripe secret key (test keys start with "sk_test_")
STRIPE_SECRET_KEY=sk_test_replace_with_your_key
# ----------------------------
# Notifications (Telegram)
# Bot token from @BotFather (format: 123456789:AA...)
TELEGRAM_BOT_TOKEN=replace-with-telegram-bot-token
# Bot username WITHOUT "@"
TELEGRAM_BOT_USERNAME=replace_with_bot_username
# Admin chat id (often negative for channels, e.g. -1001234567890)
TELEGRAM_ADMIN_CHAT_ID=-1001234567890
# ----------------------------
# MySQL (ports)
# Port on your machine (host) that forwards to the MySQL container, e.g. 3307
MYSQLDB_LOCAL_PORT=3307
# Port inside the MySQL container (default MySQL port), e.g. 3306
MYSQLDB_DOCKER_PORT=3306
# ----------------------------
# MySQL (credentials + database)
MYSQLDB_USER=replace-with-mysql-database-username
MYSQLDB_PASSWORD=replace-with-database-root-password
MYSQLDB_ROOT_PASSWORD=replace-with-database-root-password
MYSQLDB_DATABASE=replace-with-database-name
# ----------------------------
# Spring application (ports)
# Port on your machine (host) that forwards to the Spring container, e.g. 8001
SPRING_LOCAL_PORT=8001
# Port inside the Spring container (typical Spring Boot port), e.g. 8080
SPRING_DOCKER_PORT=8080
# ----------------------------
# Remote debugging (JDWP), e.g. 5050
DEBUG_PORT=5050








#MYSQLDB_LOCAL_PORT=Port-number.-For-example-3307
#MYSQLDB_DOCKER_PORT=Port-number.-For-example-3306
#MYSQLDB_USER=mysql-database-username
#MYSQLDB_PASSWORD=Username-password
#MYSQLDB_ROOT_PASSWORD=Database-root-password
#MYSQLDB_DATABASE=Database-name
#SPRING_LOCAL_PORT=Port-number.-For-example-8001
#SPRING_DOCKER_PORT=Port-number.-For-example-8080
#DEBUG_PORT=Port-number.-For-example-5050